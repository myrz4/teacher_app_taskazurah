{
  "name": "functions",
  "description": "Cloud Functions for Firebase (Taska Zurah Chat Notifications)",
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "lint": "echo Skipping lint step...",
    "deploy:billing-functions": "powershell -NoProfile -ExecutionPolicy Bypass -File ../../tools/deploy-billing-functions.ps1",
    "billplz:deploy": "powershell -NoProfile -ExecutionPolicy Bypass -File ../../tools/deploy-billing-functions.ps1",
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "test:fee-engine": "node fee-engine-check.js",
    "e2e:attendance": "npx --yes firebase-tools emulators:exec --project taskazurah --only firestore \"node e2e-attendance-check.js\"",
    "e2e:casual-transit": "npx --yes firebase-tools emulators:exec --project taskazurah --only firestore \"node e2e-casual-transit-check.js\"",
    "e2e:billing": "npx firebase-tools emulators:exec --project taskazurah --only firestore \"node e2e-billing-taska-zurah-check.js\"",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^7.2.2",
    "stripe": "^18.4.0"
  },
  "devDependencies": {
    "firebase-functions-test": "^3.1.0"
  },
  "private": true
}
